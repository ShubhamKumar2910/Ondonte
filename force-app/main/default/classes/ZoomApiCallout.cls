public class ZoomApiCallout {
    
    @Future(callout = true)
    public static void initiateZoomCall() {
        //TODO: zoom integration per event in salesforce will go here, and once successfull of invite creation in zoom we will update back the url in event record.
    }

}